using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
using DG.Tweening;

public class MapDetailMove : MonoBehaviour
{
    public Image bestrecord;
    public RectTransform mainRectTransform;


    // Start is called before the first frame update
    void Start()
    {
        //RectTransform rectTransform = bestrecord.GetComponent<RectTransform>();

        //// 높이와 너비를 출력
        //float btnwidth = rectTransform.rect.width;
        //float btnheight = rectTransform.rect.height;

        //mainRectTransform.DOAnchorPosX(-btnwidth/2, 0.5f);

    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
